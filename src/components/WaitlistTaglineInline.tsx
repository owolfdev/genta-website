import { WAITLIST_OVERLAY } from "@/lib/shellConfig";

/** Same wording as {@link WAITLIST_OVERLAY.tagline} (hero blurb; waitlist form sits directly below). */
export function WaitlistTaglineInline() {
  return <>{WAITLIST_OVERLAY.tagline}</>;
}
